import styled from 'styled-components'

const PageWidth = styled.section`
  max-width: 1500px;
  padding: 10px;
`

const Title = styled.h2`
`

const Subtitle = styled.h2`

`

export { PageWidth, Title }